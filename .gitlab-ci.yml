image: python
before_script:
  tags:
    - docker
  script:
    - pip3 install -r requirements.txt
    - python --version
    
test:
  tags:
    - docker
  script:
    - cd main
    - python manage.py test cs14.tests
